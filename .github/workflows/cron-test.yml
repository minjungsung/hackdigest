name: Cron Test

on:
  schedule:
    - cron: '*/5 * * * *'    # every 5 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Cron is alive
        run: echo "Cron triggered at $(date -u)"
